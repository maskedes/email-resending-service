{
  "name": "email-api-worker",
  "version": "1.0.0",
  "private": true,
  "description": "E-NVOY email sending API edge worker — proxy + rate limiting on Cloudflare",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types --env-interface CloudflareEnv"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
